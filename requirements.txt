# Core dependencies
transformers>=4.30.0
torch>=2.0.0
sentencepiece>=0.1.99
pydantic>=2.0.0
python-dotenv>=1.0.0
jinja2>=3.1.0
pyyaml>=6.0

# Interactive shell support (text2shell)
pexpect>=4.8.0

# Optional dependencies for specific models
# accelerate>=0.20.0  # For faster model loading
# bitsandbytes>=0.40.0  # For 8-bit quantization
